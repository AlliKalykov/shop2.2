Перед запуском проекта необходимо создать файл .env в корне проекта и заполнить его следующими переменными:

    DEBUG=
    SECRET_KEY=

    DB_NAME=
    DB_USER=
    DB_USER_PASSWORD=
    DB_HOST=

Скачайте все зависимости из файла req.txt:

    pip install -r req.txt
